report‑week5 규칙 파일 도입 전후 비교 리포트

 규칙 파일 도입 전 (CLAUDE.md, GOLDEN_RULES.md 없을 때)
【규칙이 없을 때 AI의 전체 응답을 붙여넣기: 커스텀 메뉴 기능을 구현하지만 innerHTML에 사용자 입력을 직접 이어 붙여 XSS 취약점이 존재하며, 안전 위험에 대해 능동적으로 알리지 않음】

규칙 파일 도입 후 (두 개의 md 파일을 배치하고 세션을 초기화한 뒤 동일한 질문 제시)
【규칙 파일이 있을 때 AI의 전체 응답을 붙여넣기: 먼저 CLAUDE.md, GOLDEN_RULES.md를 읽고 기존 코드의 취약점을 스캔한 뒤 innerHTML 관련 위험 3곳을 수정한다. textContent를 사용해 안전하게 렌더링하고 모든 골든 룰을 하나씩 검증한 뒤 최종 코드를 출력함】

관찰 (총 5줄)
1. 규칙 파일이 없을 때 AI는 우선적으로 기능 구현에 집중하여 커스텀 메뉴 추가·삭제, 로컬 저장 기능을 만들지만 사용자 입력으로 인한 XSS 보안 위험을 인지하지 못했다.
2. 저장소에 CLAUDE.md와 GOLDEN_RULES.md 규칙 파일이 존재하면 AI는 먼저 프로젝트 제약 조건을 읽고 코드 준수 여부 검사를 진행한 뒤 새로운 코드를 작성한다.
3. AI는 `renderCustomList`, `renderResult`, `renderHistory` 세 곳의 innerHTML 위험 지점을 직접 찾아내고 DOM API와 textContent를 사용해 XSS 주입을 막으며 규칙 파일의 보안 요구사항을 준수했다.
4. 수정이 완료된 뒤 AI는 골든 룰을 하나씩 검증했으며 데이터 출처, localStorage 저장 내용, try‑catch 예외 처리, confirm 2단계 확인이 모두 규약에 맞는지 확인했다.
5. 실험을 통해 알 수 있는 점: 규칙 파일은 AI의 기본 코딩 능력을 바꾸지는 않지만 프로젝트 전용 체크리스트를 제공하여 출력 결과가 해당 프로젝트의 보안 및 구조적 제약에 부합하도록 만든다.

