이 프로젝트는 "오늘 뭐 먹지?" 점심 메뉴 룰렛 웹페이지입니다.

 프로젝트 기본 정보
- 유일한 코드 파일: index.html, HTML·CSS·JS가 하나의 파일에 포함됨
- 외부 서드파티 라이브러리를 사용하지 않음
- localStorage를 사용해 히스토리 기록을 저장
- Canvas로 룰렛 화면을 그림
 코드 수정 시 반드시 준수할 사항
1. 코드 수정 시 전체 index.html을 출력하기보다 변경된 부분 위주로 제시한다.
2. innerHTML을 사용할 때는 반드시 XSS 위험을 검토해야 한다. 사용자 입력을 새로 추가하는 경우 textContent 사용을 우선한다.
3. 삭제, 초기화 등 되돌릴 수 없는 동작에는 confirm() 2단계 확인 팝업을 반드시 포함한다.
4. localStorage를 읽을 때 JSON.parse에는 try‑catch 예외 처리를 반드시 유지한다.
5. 새로운 기능을 추가할 때 기존에 구현된 기능이 깨지지 않아야 한다.
6. 본 프로젝트는 수업 실습용 프로토타입일 뿐 실제 서비스 제품이 아니다.
